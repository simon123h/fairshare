stages:
  - test
  - build

image: node:20

before_script:
  - corepack enable
  - corepack prepare pnpm@latest-10 --activate
  - pnpm install

unit-tests:
  stage: test
  script:
    - pnpm test

build-job:
  stage: build
  script:
    - pnpm run build
  artifacts:
    paths:
      - dist/
