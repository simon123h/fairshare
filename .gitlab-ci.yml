stages:
  - test
  - build

image: node:20

cache:
  paths:
    - node_modules/

before_script:
  - npm install

unit-tests:
  stage: test
  script:
    - npm test

build-job:
  stage: build
  script:
    - npm run build
  artifacts:
    paths:
      - dist/
