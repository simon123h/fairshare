# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.coverage

# FairShare: Berichte & PDFs ignorieren
report.md
*.pdf

# FairShare: Kosten-Daten (lokal & privat)
# We ignore all files ending in '.costs.yaml'.
# This allows you to use e.g. 'vacation.costs.yaml' without committing it.
*.costs.yaml

# Die Vorlage soll jedoch explizit eingecheckt bleiben
!costs.yaml.example

# Allgemeine Tool-Verzeichnisse
.cache/
.mypy_cache/
.ruff_cache/
build
dist
